# rag

Trụ cột 5 — RAG & cập nhật tri thức.

Chưa triển khai. Câu hỏi con 5.1 thuộc `phases/phase-1-no-train.md` (chỉ cần retriever
có sẵn, không train); câu hỏi con 5.2 thuộc `phases/phase-2-small-train.md` (cần checkpoint
backbone để so RAG với AdaPLE). Không viết code cho phần còn lại trước khi các phase phụ
thuộc trước đó hoàn tất (nguyên tắc thực thi #4 trong `plans/PLAN.md`).
