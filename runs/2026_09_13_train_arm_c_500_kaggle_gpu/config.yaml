phase: phase-2-small-train
purpose: fair 3-way comparison (Arm C), 500 steps / 0.2GB, full model config (max_seq_len=512, batch_size=8)
compute: kaggle_debug (GPU, P100, torch 2.7.1+cu126)
kernel: nguyennn263/vislm-train-arm-c-500
kernel_version: 4 (versions 1-3 failed: Kaggle mounted the dataset at the plain
                    /kaggle/input/<slug>/ path this time instead of the
                    /kaggle/input/datasets/<owner>/<slug>/ path other kernels got -
                    fixed by detecting CODE_DIR at runtime, see
                    memory/project_kaggle_environment_quirks)
dataset: HuggingFaceFW/fineweb-2, config vie_Latn, 0.2 GB streamed
tokenizer: vislm.tokenizers.blt_patch_encoder + vislm/tokenizers/syllable_seed.py
model_params: 4668608
latent_params: 3744192
device: cuda (Tesla P100-PCIE-16GB)
duration_per_step_s: ~0.399 (490 steps in 195.6s) - faster than Arm B's ~0.817s/step at
                        the same size/scale
loss_nats_per_byte_last20avg: 2.3393925070762633
loss_nats_per_byte_min: 1.9199832677841187
bits_per_byte_last20avg: 3.375
bits_per_byte_min: 2.770
compare_against: runs/2026_09_13_train_arm_b_500_kaggle_gpu (Arm B, same size/steps/data target)
note: Arm C (3.38/2.77 bpb) beats Arm B (3.58/3.12 bpb) at 500 steps too - consistent
      with the 30-step debug result. Among byte-level methods C > B; Arm A (BPE) still
      leads both in absolute bits/byte (2.05/1.86) at this scale.
