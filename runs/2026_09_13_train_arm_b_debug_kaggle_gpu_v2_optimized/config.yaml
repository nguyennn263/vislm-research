phase: phase-2-small-train
purpose: Arm B rerun after (1) compute-matching to d_model=192 and (2) batching the
         local decoder across patches - measures the real speedup on the same GPU
compute: kaggle_debug (GPU, P100, torch 2.7.1+cu126)
kernel: nguyennn263/vislm-train-arm-b-debug
kernel_version: 2
model_params: 4545728
latent_params: 3670464
device: cuda (Tesla P100-PCIE-16GB)
duration_per_step_s: ~0.256 (was ~1.63 before the decoder-batching optimization + size
                        change - see runs/2026_09_13_train_arm_b_debug_kaggle_gpu/) -
                        ~6.4x faster on the same GPU
compare_against: runs/2026_09_13_train_arm_b_debug_kaggle_gpu (pre-optimization)
