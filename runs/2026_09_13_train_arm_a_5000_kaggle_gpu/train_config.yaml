architecture: transformer_standard
arm: A
compute: rtx24gb_ssh
dataset: /kaggle/working/data/prepared/fineweb2_vi
dataset_target_gb: 0.05
model:
  d_model: 256
  max_seq_len: 512
  n_heads: 8
  n_layers: 6
phase: phase-2-small-train
resume_from: null
run_dir: /kaggle/working/runs/arm_A_5000
seed: 42
status: chưa chạy
tokenizer: vislm.tokenizers.bpe_baseline
tokenizer_name: vinai/PhoGPT-4B
train:
  batch_size: 8
  eval_every: 500
  eval_steps: 20
  grad_clip: 1.0
  lr: 0.0003
  max_steps: 5000
  min_lr: 3.0e-05
  save_every: 1000
  val_fraction: 0.05
  warmup_steps: 200
