phase: phase-2-small-train
purpose: scale-up run (Arm C), 5000 steps / 0.3GB, entropy_pretrain_steps=500
compute: kaggle (GPU, P100, torch 2.7.1+cu126)
kernel: nguyennn263/vislm-train-arm-c-5000
kernel_version: 1
model_params: 4668608
latent_params: 3744192
device: cuda (Tesla P100-PCIE-16GB)
duration_per_step_s: ~0.450 (4990 steps in 2244.8s) - SLOWER than Arm B this time
                        (0.226s/step) at the same scale, opposite of the 500-step
                        comparison where C was faster than B. Likely: as the entropy
                        model gets better-trained (500 pretrain steps), Arm B's
                        patches grow longer (fewer entropy-triggered splits), while
                        Arm C's mandatory space-boundary splits keep its patches
                        capped shorter regardless - so C ends up processing more,
                        smaller patches per sequence at this training regime.
val_loss_bits_per_byte_curve:
  step_500: 3.292
  step_1000: 3.168
  step_1500: 3.128
  step_2000: 3.092
  step_2500: 3.096
  step_3000: 3.060
  step_3500: 3.045
  step_4000: 3.017
  step_4500: 3.016
compare_against: runs/2026_09_13_train_arm_b_5000_kaggle_gpu (Arm B), runs/2026_09_13_train_arm_a_5000_kaggle_gpu (Arm A)
note: "C/A ratio also widens over training (1.67x -> 2.03x, same pattern as B/A). But
      C/B ratio (Arm C's bits/byte over Arm B's) SHRINKS toward 1.0 as training
      progresses (0.915 -> 0.956) - Arm C's advantage over Arm B, while still real
      throughout, gets smaller in relative terms as training continues. Plausible
      read: syllable-seeding gives a head start the model needs less as it learns
      more structure on its own from entropy alone. See
      phases/phase-2-small-train.md for the full A/B/C table."
