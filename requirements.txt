# Phase 0/1 — chạy local (Apple M5, CPU), chỉ cần tokenizer, không cần train.
numpy
pyyaml
transformers
tokenizers
sentencepiece
# Phase 2 data prep (streaming, không cần train) — setup/download_prepare_data.py
datasets
